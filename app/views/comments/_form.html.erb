<%= form_for([@post, @post.comments.build], remote: true) do |f| %>
  <div class="field">
    <%= f.label :comment_body %>
    <%= f.text_area :comment_body, id: :comment_comment_body %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
